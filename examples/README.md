# Examples

Standalone projects that consume the **published** `writinglint-*` packages from
npm. They are deliberately **not** part of this repo's npm workspace, so
`npm install` here pulls the real published packages — exactly what an outside
user gets, not the local source.

- [`node-lint/`](node-lint) — lint text from Node using the library API.
