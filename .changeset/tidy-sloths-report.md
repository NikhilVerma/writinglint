---
'slopsift': patch
---

Add compact output to normal CLI linting through `--format compact` or `--feedback compact`. Use `text` as the normal format name while preserving `stylish` and `detailed` as compatibility aliases.
