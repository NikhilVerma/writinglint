{"id":"abstract-summary-chain","bad":"Three setup gaps, all the same shape: the capability already existed and nobody could find it. They ship together because they form one journey through the product.","fixed":"This change fixes three setup problems:\n\n- The account switcher stayed hidden.\n- The create button appeared only on the admin page.\n- A new account could not copy an existing account.","expectedRules":["abstract-reference-chain"]}
{"id":"existential-setup","bad":"There were three problems the first time people opened the dashboard.","fixed":"The dashboard hid three controls when people first opened it.","expectedRules":["abstract-reference-chain"]}
{"id":"three-fragment-chain","bad":"Grouped into one release because each change is small. Independent of the other release work. Partially delivers the account setup ticket.","fixed":"This release contains three small account setup changes. It does not depend on the other release.","expectedRules":["fragment-chain"]}
{"id":"two-fragment-chain","bad":"Second part of the account setup work. Intended to finish the missing create flow.","fixed":"This is the second part of the account setup work. It finishes the missing create flow.","expectedRules":["fragment-chain"]}
{"id":"three-label-openers","bad":"**Typed account create.** The endpoint now accepts an account type.\n\n**A real publish control.** The page now shows a Publish button.\n\n**Names and one hidden error.** The menu has a clearer name and failed saves show an error.","fixed":"This change makes three improvements:\n\n- The endpoint accepts an account type.\n- The page shows a Publish button.\n- The menu has a clearer name, and failed saves show an error.","expectedRules":["label-led-explanation"]}
{"id":"two-label-openers","bad":"**Queue selection.** The worker uses its project queue.\n\n**Error reporting.** Failed tasks now show their error.","fixed":"The worker uses its project queue. Failed tasks now show their error.","expectedRules":["label-led-explanation"]}
{"id":"three-parenthetical-explanations","bad":"The switcher stayed hidden (although the team could already use it). The create button remained on the admin page (where most people never looked for it). The copy option was missing (this part required new code).","fixed":"The switcher stayed hidden. The create button remained on the admin page. The copy option did not exist.","expectedRules":["aside-pileup"]}
{"id":"one-large-parenthetical","bad":"The retry limit is five (including the first request and four later attempts).","fixed":"The system makes at most five requests. The first request and four retries count toward that limit.","expectedRules":["aside-pileup"]}
{"id":"strict-sentence-limit","bad":"The technician opened the cabinet and checked every cable in the upper tray before recording the serial numbers in the maintenance log for the team that would return during the next shift.","fixed":"The technician opened the cabinet and checked every cable in the upper tray. The technician recorded the serial numbers for the next shift.","expectedRules":["sentence-load"]}
{"id":"short-but-compressed-sentence","bad":"Each dependency was checked in isolation: the database, cache, search, and worker ports were disabled (by pointing them at a closed local port) — all passed, so hidden services could not affect the result.","fixed":"We disabled every service before the test. The database, cache, search, and worker ports pointed to a closed local port. Every test passed.","expectedRules":["sentence-load","aside-pileup"]}
